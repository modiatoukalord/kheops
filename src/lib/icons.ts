import { Book, BookCopy, Puzzle, Gamepad2, MicVocal, User, FileSignature, DollarSign, Tag, HandCoins, Ticket, Percent, PlusCircle } from 'lucide-react';

export const iconMap = {
    Book: Book,
    BookCopy: BookCopy,
    Puzzle: Puzzle,
    Gamepad2: Gamepad2,
    MicVocal: MicVocal,
    User: User,
    FileSignature: FileSignature,
    DollarSign: DollarSign,
    Tag: Tag,
    HandCoins: HandCoins,
    Ticket: Ticket,
    Percent: Percent,
    PlusCircle: PlusCircle,
};

export const iconList = Object.keys(iconMap);

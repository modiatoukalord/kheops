import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function SiteSettings() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Paramètres du Site</CardTitle>
        <CardDescription>
          Configurez les paramètres généraux de la plateforme KHEOPS.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Les paramètres du site seront bientôt disponibles.</p>
      </CardContent>
    </Card>
  );
}
